import styled from "styled-components";

const Button = styled.button`
  background: #2faac4;
  color: white;
  padding: 10px;
  border: none;
  font-size: 16px;
  margin: 10px auto;
  font-weight: bold;
  cursor: pointer;
`;

export default Button;
